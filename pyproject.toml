[project]
name = "ai-backend-bootcamp"
version = "0.1.0"
description = "A structured AI-assisted backend and full-stack development bootcamp."
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "alembic>=1.19.1,<2",
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.4,<4",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0,<3",
    "sqlalchemy>=2.0.52,<2.1",
    "uvicorn[standard]>=0.52.1",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["app", "ticket_api", "ticket_rules"]

[dependency-groups]
dev = [
    "httpx2>=2.10.0",
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
